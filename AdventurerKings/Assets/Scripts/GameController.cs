using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameController : MonoBehaviour {
    public List<Market> Markets = new List<Market>();
    public Player player;
    public Market CurrentMarket;
    public stuf make the stuff happen t


	// Use this for initialization
	void Start () {
		
	}


    public void StartGame() {
        /*GameObject go = GameObject.CreatePrimitive(PrimitiveType.Cube); 

        go.AddComponent<Market>().SetupMarket("Wishmerhill", Random.Range(1,7),Random.Range(0,15));
        go.GetComponent<Market>().xpos = 0;
        go.GetComponent<Market>().ypos = 0;
        go.transform.position = new Vector3(0, 0, 0);
        */

    }
	// Update is called once per frame
	void Update () {
		
	}
}
